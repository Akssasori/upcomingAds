server:
  port: 8970

spring:
  application:
    name: upcomingAds
  output:
    ansi:
      enabled: ALWAYS
  mvc:
    async:
      request-timeout: 80000
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


api:
  elevenlabs:
    key: ${ELEVENLABS_API_KEY}




